I have implemented the GitHub Actions workflow in the .github/workflows/ci.yml file.

It runs mvn clean test and mvn package on every push to the main branch, and uploads the target/ folder as a build artifact.